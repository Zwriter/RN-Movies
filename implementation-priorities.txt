Core IMDB-like app: implemented vs missing (priorities)

Overview (what's present):
- Models: Movie, Genre, Person, Image, Review, User, MoviePerson
- Livewire pages/components: Welcome (top-rated), Movies (listing with filters), Movie (detail), MoviePostCard
- Ratings aggregation: `Movie::rating` accessor (avg of reviews)
- Database seeders & factories for users, genres, movies, people, images, reviews
- Basic search/filtering/sorting in `App\Livewire\Movies`
- Routes for top rated and movie detail

Major missing or inconsistent items (high priority):
1) Authentication & User accounts
   - No auth routes/UI (register/login/logout, profiles)
   - Needed for reviews, watchlist, lists, moderation
   - Priority: HIGH

2) Review posting & rating UI + API
   - No UI or controller to create/edit/delete reviews by authenticated users
   - Upvote/downvote for reviews missing
   - Priority: HIGH

3) Watchlist / Watched history / Favorites
   - `movies.favorites` field exists but no user-scoped watchlist or watched history
   - Implement pivot tables (`user_movie` with status/watchlist/watched) and UI
   - Priority: HIGH

4) Admin CRUD and moderation
   - No admin routes or authorization checks for creating/editing movies, genres, people
   - Seed has admin flag; add middleware + admin UI
   - Priority: HIGH

5) Model relation fixes & view bugs
   - Some minor bugs exist (e.g., `genres->pluck('name')` vs `genre/title`) and leftover relation names (Post/Comment)
   - Priority: HIGH (fixes required before new features)

6) Genre detail page and routes
   - `genres.show` route not defined (welcome view linked to /movies as fallback)
   - Add genre page showing movies in that genre
   - Priority: MEDIUM

7) Full-text search & discovery improvements
   - Currently basic title filtering; add actor/director search, indexing (Scout/Meilisearch/Algolia)
   - Priority: MEDIUM

8) Recommendations / Similar titles
   - Not implemented; build algorithm (genre overlap + collaborative filtering later)
   - Priority: MEDIUM

9) Lists & organization (user lists, public/private)
   - No UI or models for user lists
   - Priority: MEDIUM

10) Media attachments (trailers, additional images)
    - `Image` model exists but no attachments for trailers; implement Video model or external embed support
    - Priority: LOW

11) Tests, CI, and docs
    - Add feature tests for auth, reviews, watchlist; document API and setup steps
    - Priority: MEDIUM

Proposed immediate next steps (implementation sprint):
A. Fix model/view inconsistencies and clean unused relation references (low friction, unblock work)
B. Scaffold authentication (Laravel Breeze or Fortify) and basic profile pages
C. Add review creation endpoints + UI (protected by auth) and wire rating aggregation
D. Implement user watchlist (pivot table) and a small UI (add/remove, watched toggle)
E. Add simple admin middleware + CRUD for movies/genres (using resource controllers or Livewire)

Deliverables I'll create now:
- `implementation-priorities.txt` (this file)
- A short, ordered todo list in the repo to track the first sprint (fixes → auth → reviews → watchlist → admin)

If you confirm, I'll start with step A: fix model/view inconsistencies and remove leftover `Post`/`Comment` references, then create the `genres.show` route and a basic genre view.
